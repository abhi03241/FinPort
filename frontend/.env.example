# Copy to .env.local and adjust as needed.
# Vite proxies /api, /actuator, /authenticateTheUser and /logout to the backend
# (see vite.config.ts), so you typically don't need to change anything for local dev.

# VITE_API_BASE_URL=http://localhost:8080/api/v1